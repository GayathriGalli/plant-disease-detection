# Sample Plant Leaf Images

This directory contains sample plant leaf images that can be used to test the Plant Disease Detection application.

Upload a leaf image through the Streamlit application to classify the plant disease using the trained deep learning model.

> Note: Only sample/test images should be stored here. The complete training dataset is not included in this repository because of its size.
