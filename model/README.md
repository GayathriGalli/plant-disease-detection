# Trained Model

This directory is used to store the trained Plant Disease Detection model and its class labels.

After running `train_model.py`, the following files will be generated locally:

- `plant_disease_model.keras` — trained TensorFlow/Keras model
- `class_names.txt` — plant disease class labels

The trained model file is excluded from Git tracking because model artifacts can be large.
